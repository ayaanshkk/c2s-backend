# Minimal dependencies to run the c2s-backend Flask API (local dev + typical deploy).
# Use Python 3.11+. On Windows, the full requirements.txt often fails (PyQt5 pins, etc.).
#
# Install:  py -3.11 -m venv .venv
#           .\.venv\Scripts\pip install -r requirements-c2s-app.txt

Flask==3.1.1
flask-cors==6.0.1
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.43
psycopg2-binary
python-dotenv==1.1.1
PyJWT==2.8.0
Werkzeug==3.1.3
gunicorn==21.2.0
cloudinary==1.41.0
requests==2.32.5
httpx==0.28.1
